[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "litec-wam-public"
version = "0.1.0"
description = "Public statistics and policy-interface tools for LiteC-WAM"
requires-python = ">=3.10"
license = {file = "LICENSE"}
readme = "README.md"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
